<div class="container-fluid d-flex justify-content-center">
  {{#if posts.length}}
    {{#each posts as |post|}}
      <div class="col-7">
        {{> post-details}}
      </div>
    {{/each}}
  {{else}}
    <div class="text-center mt-5">
      <h2 class="pt-5">No posts yet...</h2>
    </div>
  {{/if}}
</div>